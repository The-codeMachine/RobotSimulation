# Utility

Utility is a small header file that defines some random utility functions. Some examples include: `assertNear` which
tests if two values are similar in value, allowing an EPSILON for freedom. 